## Task

TASK-XXX — titolo

## Cosa cambia

Due o tre righe. Cosa funziona adesso che prima non funzionava.

## Come è stato verificato

- [ ] `pytest` verde in locale
- [ ] `ruff` e `black` puliti
- [ ] **Fase 1**: GPX generato, guardato in un visualizzatore, salvato in
      `samples/` e annotato in `samples/LOG.md`

Zone provate, se pertinente: `trento` / `levico` / `valsugana`

Campioni aggiunti in questa PR:

```
samples/TASK-XXX_forma_distanza_zona_vN.gpx
```

## Cosa resta fuori

Ciò che è emerso e non è stato fatto qui, con il task o la nota dove è
stato annotato.

## Prima del merge

- [ ] `docs/STATUS.md` aggiornato
- [ ] Nuova voce in `docs/DECISIONS.md`, se è stata presa una decisione
- [ ] Il file del task è passato a `Done`, con l'esito compilato
- [ ] Nessun segreto nel diff
- [ ] Ho riletto il diff per intero
