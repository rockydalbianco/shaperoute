"""Shape generators on the unit square. Generators arrive with TASK-011."""

# Shapes accepted by RouteRequest. A shape is listed here only once the
# contract (docs/ARCHITECTURE.md §3) names it.
SUPPORTED_SHAPES: tuple[str, ...] = ("circle", "heart")
